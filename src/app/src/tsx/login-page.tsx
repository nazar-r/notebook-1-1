import RenderingButtons from "./launching-buttons";

const LoginPageContent = () => (
    <div className="login-page">
        <div className="login-page__title">Welcome Back!</div>
        <RenderingButtons />
    </div>
);

export default LoginPageContent;